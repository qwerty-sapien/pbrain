[project]
name = "productivebrain"
version = "0.1.1"
description = "Terminal-native study and deliberate-practice system"
readme = "README.md"
license = "AGPL-3.0-or-later"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
    "structlog>=24.1.0",
    "tomli>=2.0.1",
    "pydantic>=2.5.0",
    "mcp>=1.27.0",
    "cryptography>=46.0.0",
    "keyring>=25.0.0",
    "pyyaml>=6.0",
    "httpx>=0.28.0",
    "prompt-toolkit>=3.0.52",
]

[project.scripts]
pb = "pb.cli.main:run_app"
pbrain = "pb.cli.main:run_app"
brain = "pb.cli.main:run_app"
pb-mcp = "pb.mcp.server:main"

[project.optional-dependencies]
anki = [
    "genanki",
]
batch = [
    "google-cloud-storage>=2.0.0",
    "google-genai>=1.0.0",
]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
]
embeddings = [
    "sqlite-vec>=0.1.9",
]
gemini = [
    "google-genai>=1.0.0",
]
google = [
    "google-api-python-client>=2.194.0",
    "google-auth-oauthlib>=1.3.1",
]
kaggle = [
    "kaggle>=2.1.0",
]
pdf = [
    "pypdf>=4.0.0",
]
scraping = [
    "beautifulsoup4>=4.12.0",
    "feedparser>=6.0.12",
    "lxml>=5.0.0",
]
watch = [
    "watchdog>=6.0.0",
]
all = [
    "beautifulsoup4>=4.12.0",
    "feedparser>=6.0.12",
    "genanki",
    "google-api-python-client>=2.194.0",
    "google-auth-oauthlib>=1.3.1",
    "google-cloud-storage>=2.0.0",
    "google-genai>=1.0.0",
    "kaggle>=2.1.0",
    "lxml>=5.0.0",
    "pypdf>=4.0.0",
    "sqlite-vec>=0.1.9",
    "watchdog>=6.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pb"]
include = [
    "/src/pb/domain_packs/*.yaml",
    "/src/pb/templates/*.md",
    "/src/pb/cli/glow_style.json",
]

[tool.hatch.build.targets.sdist]
include = [
    "/src/pb",
    "/docs",
    "/README.md",
    "/NOTICE",
    "/LICENSE",
    "/AGENTS.md",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-v --tb=short"
